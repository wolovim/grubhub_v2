<h1>ITEMS!</h1>
<% @items.each do |item| %>
	<% item.categories.each do |category| %>
		<%= category.name %>
	<% end %>
	<%= link_to item.title, item %>
	<%= item.description %>
	<%= item.price %>
<% end %>
